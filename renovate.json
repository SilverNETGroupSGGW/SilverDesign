{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "rangeStrategy": "bump",
  "prHourlyLimit": 0,
  "lockFileMaintenance": {
    "enabled": true
  },
  "packageRules": [
    {
      "description": "One PR, so CI and the brochure render check the non-major updates together; majors keep a PR each.",
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "groupName": "non-major dependencies",
      "groupSlug": "non-major"
    },
    {
      "description": "astro check needs TypeScript's programmatic API, which the native 7.x compiler does not ship yet (withastro/roadmap discussion 1321).",
      "matchPackageNames": ["typescript"],
      "allowedVersions": "<7"
    }
  ]
}

import * as React from 'react';

/**
 * Eyebrow — from @silver/ui@0.1.0.
 */
export interface EyebrowProps {
  className?: string;
  style?: React.CSSProperties;
  children?: React.ReactNode;
}

export declare const Eyebrow: React.ComponentType<EyebrowProps>;
